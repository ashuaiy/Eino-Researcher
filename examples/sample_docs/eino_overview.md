# Eino Overview

Eino is a Go framework for building LLM applications. In this project it is planned as the primary orchestration framework for chat models, tools, retrievers, agents, and workflows.

The first milestone keeps Eino integration behind interfaces so the HTTP API, configuration, database schema, and deployment layout can stabilize before complex agent logic is added.
